секція РМв1 {
  синонім памʼять = адреса;

  структура Назва;
  структура Обʼєкт;
  структура Середовище;
  структура Виконувач;
  структура Розширення;

  структура Результат {
    успіх: логічне;
    значення: адреса<Обʼєкт>;
  }

  структура ВерсіяРозширення {
    мажор: позитивне;
    мінор: позитивне;
    патч: позитивне;
  }

  структура Розширення {
    версія: ВерсіяРозширення;
    В: адреса<Виконувач>;
    обʼєкт_модуля: адреса<Обʼєкт>;
    дані: адреса;
    надрукувати_ю8: (значення: памʼять<п8>, Р: адреса<Розширення>) -> ніщо;
    надрукувати_обʼєкт: (обʼєкт: адреса<Обʼєкт>, Р: адреса<Розширення>, глибина: позитивне) -> ніщо;
  }

  дія отримати_обʼєкт_модуля(Р: адреса<Розширення>): адреса<Обʼєкт> {
    вернути Р.обʼєкт_модуля;
  }

  дія записати_дані_розширення(Р: адреса<Розширення>, дані: адреса) {
    Р.дані = дані;
  }

  дія надрукувати_ю8(значення: памʼять<п8>, Р: адреса<Розширення>) {
    Р.надрукувати_ю8(значення, Р);
  }

  дія надрукувати_обʼєкт(обʼєкт: адреса<Обʼєкт>, Р: адреса<Розширення>, глибина: позитивне) {
    Р.надрукувати_обʼєкт(обʼєкт, Р, глибина);
  }
}

////
// Приклад дії завантаження РМв1:
//
// зовнішня дія завантажити_РМв1(Р: адреса<РМв1::Розширення>) {
//   РМв1::записати_дані_розширення(Р, пусто);
//   ціль обʼєкт_модуля = РМв1::отримати_обʼєкт_модуля(Р);
// }
////
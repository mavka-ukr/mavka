взяти пак інструменти_тестування

значення = []
інструменти_тестування.впасти_якщо_не_рівно(значення.довжина, 0)

значення.додати(1)
інструменти_тестування.впасти_якщо_не_рівно(значення.довжина, 1)
інструменти_тестування.впасти_якщо_не_рівно(значення[0], 1)
інструменти_тестування.впасти_якщо_списки_не_рівні(значення, [1])

значення.додати("2")
інструменти_тестування.впасти_якщо_не_рівно(значення.довжина, 2)
інструменти_тестування.впасти_якщо_не_рівно(значення[1], "2")
інструменти_тестування.впасти_якщо_списки_не_рівні(значення, [1, "2"])
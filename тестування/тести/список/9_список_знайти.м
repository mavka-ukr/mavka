взяти пак інструменти_тестування

значення = [1, "2", так, ні]
інструменти_тестування.впасти_якщо_не_рівно(значення.довжина, 4)

знайдене = значення.знайти((ел): ел)
інструменти_тестування.впасти_якщо_не_рівно(знайдене, 1)

знайдене = значення.знайти((ел): ел є текст)
інструменти_тестування.впасти_якщо_не_рівно(знайдене, "2")

знайдене = значення.знайти((ел): ел є логічне)
інструменти_тестування.впасти_якщо_не_рівно(знайдене, так)
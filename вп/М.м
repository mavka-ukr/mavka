js """
м_Е = Math.E;
м_ЛОГ10 = Math.LN10;
м_ЛОГ2 = Math.LN2;
м_ЛОГ2Е = Math.LOG2E;
м_ЛОГ10Е = Math.LOG10E;
м_ПІ = Math.PI;
м_КОРІНЬ1_2 = Math.SQRT1_2;
м_КОРІНЬ2 = Math.SQRT2;
м_абс = мДія(
  "абс",
  [мПарм("а", м_число)],
  function(args) {
    const value = args[0] ?? args["а"];
    return Math.abs(value);
  }
);
м_арккос = мДія(
  "арккос",
  [мПарм("а", м_число)],
  function(args) {
    const value = args[0] ?? args["а"];
    return Math.acos(value);
  }
);
м_арксин = мДія(
  "арксин",
  [мПарм("а", м_число)],
  function(args) {
    const value = args[0] ?? args["а"];
    return Math.asin(value);
  }
);
м_арктан = мДія(
  "арктан",
  [мПарм("а", м_число)],
  function(args) {
    const value = args[0] ?? args["а"];
    return Math.atan(value);
  }
);
м_арктан2 = мДія(
  "арктан2",
  [мПарм("а", м_число), мПарм("б", м_число)],
  function(args) {
    const value = args[0] ?? args["а"];
    const value2 = args[1] ?? args["б"];
    return Math.atan2(value, value2);
  }
);
м_до_більшого = мДія(
  "до_більшого",
  [мПарм("а", м_число)],
  function(args) {
    const value = args[0] ?? args["а"];
    return Math.ceil(value);
  }
);
м_до_меншого = мДія(
  "до_меншого",
  [мПарм("а", м_число)],
  function(args) {
    const value = args[0] ?? args["а"];
    return Math.floor(value);
  }
);
м_кос = мДія(
  "кос",
  [мПарм("а", м_число)],
  function(args) {
    const value = args[0] ?? args["а"];
    return Math.cos(value);
  }
);
м_експ = мДія(
  "експ",
  [мПарм("а", м_число)],
  function(args) {
    const value = args[0] ?? args["а"];
    return Math.exp(value);
  }
);
м_найбільше = мДія(
  "найбільше",
  [мПарм("а", м_список)],
  function(args) {
    const value = args[0] ?? args["а"];
    return Math.max(...value);
  }
);
м_найменше = мДія(
  "найменше",
  [мПарм("а", м_список)],
  function(args) {
    const value = args[0] ?? args["а"];
    return Math.min(...value);
  }
);
м_степінь = мДія(
  "степінь",
  [мПарм("а", м_число), мПарм("б", м_число)],
  function(args) {
    const value = args[0] ?? args["а"];
    const value2 = args[1] ?? args["б"];
    return Math.pow(value, value2);
  }
);
м_округлити = мДія(
  "округлити",
  [мПарм("а", м_число)],
  function(args) {
    const value = args[0] ?? args["а"];
    return Math.round(value);
  }
);
м_син = мДія(
  "син",
  [мПарм("а", м_число)],
  function(args) {
    const value = args[0] ?? args["а"];
    return Math.sin(value);
  }
);
м_корінь = мДія(
  "корінь",
  [мПарм("а", м_число)],
  function(args) {
    const value = args[0] ?? args["а"];
    return Math.sqrt(value);
  }
);
м_тан = мДія(
  "тан",
  [мПарм("а", м_число)],
  function(args) {
    const value = args[0] ?? args["а"];
    return Math.tan(value);
  }
);
"""

макет субʼєкт Е число
макет субʼєкт ЛОГ10 число
макет субʼєкт ЛОГ2 число
макет субʼєкт ЛОГ2Е число
макет субʼєкт ЛОГ10Е число
макет субʼєкт ПІ число
макет субʼєкт КОРІНЬ1_2 число
макет субʼєкт КОРІНЬ2 число
макет дія абс(а число) число
макет дія арккос(а число) число
макет дія арксин(а число) число
макет дія арктан(а число) число
макет дія арктан2(а число, б число) число
макет дія до_більшого(а число) число
макет дія до_меншого(а число) число
макет дія кос(а число) число
макет дія експ(а число) число
макет дія найбільше(...значення список) число
макет дія найменше(...значення список) число
макет дія степінь(а число, б число) число
макет дія округлити(а число) число
макет дія син(а число) число
макет дія корінь(а число) число
макет дія тан(а число) число

дати Е
дати ЛОГ10
дати ЛОГ2
дати ЛОГ2Е
дати ЛОГ10Е
дати ПІ
дати КОРІНЬ1_2
дати КОРІНЬ2
дати абс
дати арккос
дати арксин
дати арктан
дати арктан2
дати до_більшого
дати до_меншого
дати кос
дати експ
дати найбільше
дати найменше
дати степінь
дати округлити
дати син
дати корінь
дати тан
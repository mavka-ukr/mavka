дія друк(а)
  global.console.log(а)
кінець

дія читати(запитання)
  global.diia_read(запитання)
кінець

дія об'єкт()
  jsNew(global.DiiaObject, масив(аргументи))
кінець

дія масив()
  jsNew(global.DiiaArray, аргументи)
кінець

дія діапазон(початок, кінець_, крок)
  global.diia_range(початок, кінець_, крок)
кінець

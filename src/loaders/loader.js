class Loader {
  constructor(mavka) {
    this.mavka = mavka;
  }

  /**
   * Load a module.
   *
   * @param {Context} context
   * @param {string} name
   * @param {boolean} pak
   */
  async load(context, name, pak = false) {
  }
}

export default Loader;

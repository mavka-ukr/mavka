class Instruction {
  constructor(mavka) {
    this.mavka = mavka;
  }

  /**
   * @param {Scope} scope
   * @param {ASTNode} node
   * @param {*} options
   */
  async compile(scope, node, options) {

  }
}

export default Instruction;

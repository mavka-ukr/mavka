js """
var м_Е = Math.E;
var м_ЛОГ10 = Math.LN10;
var м_ЛОГ2 = Math.LN2;
var м_ЛОГ2Е = Math.LOG2E;
var м_ЛОГ10Е = Math.LOG10E;
var м_ПІ = Math.PI;
var м_КОРІНЬ1_2 = Math.SQRT1_2;
var м_КОРІНЬ2 = Math.SQRT2;
var м_абс = мДія(
  function абс(м_а) {
    return Math.abs(м_а);
  }
);
var м_арккос = мДія(
  function арккос(м_а) {
    return Math.acos(м_а);
  }
);
var м_арксин = мДія(
  function арксин(м_а) {
    return Math.asin(м_а);
  }
);
var м_арктан = мДія(
  function арктан(м_а) {
    return Math.atan(м_а);
  }
);
var м_арктан2 = мДія(
  function арктан2(м_а, м_б) {
    return Math.atan2(м_а, м_б);
  }
);
var м_до_більшого = мДія(
  function до_більшого(м_а) {
    return Math.ceil(м_а);
  }
);
var м_до_меншого = мДія(
  function до_меншого(м_а) {
    return Math.floor(м_а);
  }
);
var м_кос = мДія(
  function кос(м_а) {
    return Math.cos(м_а);
  }
);
var м_експ = мДія(
  function експ(м_а) {
    return Math.exp(м_а);
  }
);
var м_найбільше = мДія(
  function найбільше(м_а) {
    return Math.max(...м_а);
  }
);
var м_найменше = мДія(
  function найменше(м_а) {
    return Math.min(...м_а);
  }
);
var м_степінь = мДія(
  function степінь(м_а, м_б) {
    return Math.pow(м_а, м_б);
  }
);
var м_округлити = мДія(
  function округлити(м_а) {
    return Math.round(м_а);
  }
);
var м_син = мДія(
  function син(м_а) {
    return Math.sin(м_а);
  }
);
var м_корінь = мДія(
  function корінь(м_а) {
    return Math.sqrt(м_а);
  }
);
var м_тан = мДія(
  function тан(м_а) {
    return Math.tan(м_а);
  }
);
"""

макет субʼєкт Е число
макет субʼєкт ЛОГ10 число
макет субʼєкт ЛОГ2 число
макет субʼєкт ЛОГ2Е число
макет субʼєкт ЛОГ10Е число
макет субʼєкт ПІ число
макет субʼєкт КОРІНЬ1_2 число
макет субʼєкт КОРІНЬ2 число
макет дія абс(а число) число
макет дія арккос(а число) число
макет дія арксин(а число) число
макет дія арктан(а число) число
макет дія арктан2(а число, б число) число
макет дія до_більшого(а число) число
макет дія до_меншого(а число) число
макет дія кос(а число) число
макет дія експ(а число) число
макет дія найбільше(значення список<число>) число
макет дія найменше(значення список<число>) число
макет дія степінь(а число, б число) число
макет дія округлити(а число) число
макет дія син(а число) число
макет дія корінь(а число) число
макет дія тан(а число) число

дати Е
дати ЛОГ10
дати ЛОГ2
дати ЛОГ2Е
дати ЛОГ10Е
дати ПІ
дати КОРІНЬ1_2
дати КОРІНЬ2
дати абс
дати арккос
дати арксин
дати арктан
дати арктан2
дати до_більшого
дати до_меншого
дати кос
дати експ
дати найбільше
дати найменше
дати степінь
дати округлити
дати син
дати корінь
дати тан
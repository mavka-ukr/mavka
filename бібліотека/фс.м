js """
var { default: fs } = await import("fs");

var м_прочитати = мДія(
  async function прочитати(м_шлях) {
    return await fs.promises.readFile(м_шлях) ?? null;
  }
);
var м_прочитати_текст = мДія(
  async function прочитати_текст(м_шлях) {
    return await fs.promises.readFile(м_шлях, "utf8") ?? null;
  }
);
var м_записати = мДія(
  async function записати(м_шлях, м_дані) {
    return await fs.promises.writeFile(м_шлях, м_дані) ?? null;
  }
);
var м_дописати = мДія(
  async function дописати(м_шлях, м_дані) {
    return await fs.promises.appendFile(м_шлях, м_дані) ?? null;
  }
);
var м_перейменувати = мДія(
  async function перейменувати(м_шлях, м_новий_шлях) {
    return await fs.promises.rename(м_шлях, м_новий_шлях) ?? null;
  }
);
var м_видалити = мДія(
  async function видалити(м_шлях) {
    return await fs.promises.unlink(м_шлях);
  }
);
var м_створити_папку = мДія(
  async function створити_папку(м_шлях) {
    return await fs.promises.mkdir(м_шлях);
  }
);
var м_створити_папку_рекурсивно = мДія(
  async function створити_папку_рекурсивно(м_шлях) {
    return await fs.promises.mkdir(м_шлях, { recursive: true });
  }
);
var м_видалити_папку = мДія(
  async function видалити_папку(м_шлях) {
    return fs.rmdirSync(м_шлях);
  }
);
var м_видалити_папку_рекурсивно = мДія(
  async function видалити_папку_рекурсивно(м_шлях) {
    return fs.rmdirSync(м_шлях, { recursive: true });
  }
);
var м_існує = мДія(
  async function існує(м_шлях) {
    return fs.existsSync(м_шлях);
  }
)
"""

макет тривала дія прочитати(шлях текст) байти
макет тривала дія прочитати_текст(шлях текст) текст
макет тривала дія записати(шлях текст, дані байти або текст) пусто
макет тривала дія дописати(шлях текст, дані байти або текст) пусто
макет тривала дія перейменувати(шлях текст, новий_шлях текст) пусто
макет тривала дія видалити(шлях текст) пусто
макет тривала дія створити_папку(шлях текст) пусто
макет тривала дія створити_папку_рекурсивно(шлях текст) пусто
макет тривала дія видалити_папку(шлях текст) пусто
макет тривала дія видалити_папку_рекурсивно(шлях текст) пусто
макет тривала дія існує(шлях текст) логічне

дати прочитати
дати прочитати_текст
дати записати
дати дописати
дати перейменувати
дати видалити
дати створити_папку
дати створити_папку_рекурсивно
дати видалити_папку
дати видалити_папку_рекурсивно
дати існує
